<% if book.favorited_by? current_user %>
	<%= link_to "❤︎ いいね!", book_favorites_path(book), method: :delete, remote: true %>
		 <%#= books.favorites.count %>
<% else %>
	<%= link_to "♡ いいね!", book_favorites_path(book), method: :post, remote: true  %>
		 <%#= books.favorites.count %>
<% end %>