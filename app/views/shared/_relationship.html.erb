<% if user.followed_by?(current_user) %>
	<%= link_to "follow解除", user_relationships_path(user), method: :delete, remote: true %>
<% else %>
	<%= link_to "followする", user_relationships_path(user), method: :post, remote: true %>	
<% end %>