<div class="container box">
        
        <p id="notice"></p>
      
      	<div class="row">
      		<div class="col-xs-3">
      			<%= render 'shared/userinfo', user: current_user %>
				<%= render 'shared/newbook', book: Book.new %>
      		</div>
				
			<div class="col-xs-9">
			    <h1>Book detail</h1>
			    	<!-- <table class="table table-hover table-inverse">
					      <tbody>
					      	<tr>
					      		<th></th>
							    <td><%#= book.title %></td>
							 	<td><%#= book.opinion %></td>
							 	<td><%#= link_to 'Edit', edit_book_path %></td>
							 	<td><%#= link_to 'Destroy', book_path, method: :delete %></td>
						    	<th colspan="3"></th>
						    </tr>
					      </tbody>
			    	</table> -->
			    <h3>タイトル</h3>
			    <h2><%= @book.title %></h2>
			    <hr>
			    <h3>説明</h3>
			    <p><%= @book.opinion %></p>
			    <hr>
			    <h4>投稿者</h4>
			    <% if @book.user.image? %>
				    <%= image_tag @book.user.image.url %>	
				    <% else %>
				    <%= image_tag 'no_image.jpg' %>
			    <% end %>
			    <h4 id="favorite-btn-<%=@book.id%>"><%= render 'favorites/favorite', book: @book %></h4>
			    <div>
			    	<% if @book.user.present? %>
			    		<%= link_to @book.user.name, user_path(@book.user_id) %>
			    	<% else %>
			    		ユーザーはいません
			    	<% end %>
			    	<% if current_user.id == @book.user_id %>
						<%= link_to 'Edit', edit_book_path(@book.id) %>
						<%= link_to 'Destroy', book_path(@book.id), method: :delete, data: { confirm: "本当に削除しますか？" } %>
					<% end %>
			    </div>
			</div>
		</div>
</div>